# /public/certificates

Guinness World Records certificate scans go here once available. Each card
in `components/certificates/Certificates.tsx` currently links straight to
the record's official Guinness World Records verification page (see
`lib/records.ts`) — once you have a scan for a given record, swap that
record's `PlaceholderImage` for the real scan image.
